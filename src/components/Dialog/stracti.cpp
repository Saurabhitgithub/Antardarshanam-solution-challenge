#include<iostream>
using namespace std;
struct Rectangle{
    int length;
    int breadth;
}
int main(){
    struct Rectangle *p;
(struct rectangle*) mallac(size of (struct Rectangle))
r.length =10;

(p*)breadth;
}
